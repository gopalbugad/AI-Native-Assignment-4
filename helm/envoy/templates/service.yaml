apiVersion: v1
kind: Service
metadata:
  name: envoy
spec:
  type: {{ .Values.service.type }}
  selector:
    app: envoy
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 10000
      nodePort: {{ .Values.service.nodePort }}